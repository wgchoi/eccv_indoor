function moves = preprocessJumpMoves(x, iclusters)

% moves : for each jump type : 
%       add, delete,            % no cahce required.
%       switch, combine, break
%       1. src cluster ID
%       2. dest cluster ID
%       3. required cluster IDs
%       4. cache for liklihood computation?

moves = cell(5, 1);
for i = 1:length(iclusters)
end

end